<Page
    x:Class="MineS.SetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MineS"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:behaviors="using:Microsoft.Toolkit.Uwp.UI.Animations.Behaviors"
    xmlns:UI="using:Microsoft.Advertising.WinRT.UI"
    mc:Ignorable="d" FontFamily="Bahnschrift Light" >
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary x:Name="ioh"  Source="ms-appx:///Resouse/Dictionary.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
       
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="618*"/>
                <ColumnDefinition Width="482*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="282*"/>
                <RowDefinition Height="618*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="MineS" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="54" FontFamily="Bahnschrift Light"/>
            <Grid Grid.Row="1" Margin="25" Background="{ThemeResource SuperMidAcrylicELBrush}" CornerRadius="54,54,54,54">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button Background="{ThemeResource SuperHighAcrylicELBrush}"  HorizontalAlignment="Stretch"  Click="Easy_Click" VerticalAlignment="Stretch" Margin="36">
                    <Image Source="Assets/Easy.png" Stretch="UniformToFill"/>
                </Button>
                <Button Background="{ThemeResource SuperHighAcrylicELBrush}"   HorizontalAlignment="Stretch" Click="Mid_Click" Grid.Column="1" VerticalAlignment="Stretch" Margin="36" Style="{StaticResource ButtonStyle}">
                    <Image Source="Assets/Mid.png" Stretch="UniformToFill"/>
                </Button>
                <Button Background="{ThemeResource SuperHighAcrylicELBrush}"  HorizontalAlignment="Stretch" Click="Hatd_Click" Grid.Row="1"  Margin="36" VerticalAlignment="Stretch">
                    <Image Source="Assets/Hard.png" Stretch="UniformToFill"/>
                </Button>
                <Button Background="{ThemeResource SuperHighAcrylicELBrush}"  Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" Click="Button_Click"  Margin="36" VerticalAlignment="Stretch">
                    <Image Source="Assets/Customize.png" Stretch="UniformToFill"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="1">
                <Button  Click="Button_Click_1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <SymbolIcon Symbol="Setting" />
                </Button>
            </Grid>
            <UI:AdControl Grid.Row="0" Grid.Column="1"
                              ApplicationId="9plcjb2gqn1t"
                              AdUnitId="341043"
                              HorizontalAlignment="Center"
                              Height="250"
                              VerticalAlignment="Top"
                              Width="300"/>
        </Grid>
        <Grid x:Name="Cur" Visibility="Collapsed" Grid.RowSpan="2" Background="{ThemeResource SystemControlAcrylicElementMediumHighBrush}" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <TextBlock   HorizontalAlignment="Center" Text="Height" VerticalAlignment="Center" FontSize="18"  ></TextBlock>
            <TextBlock Grid.Row="1"  HorizontalAlignment="Center" Text="Width" VerticalAlignment="Center" FontSize="18" ></TextBlock>
            <TextBlock Grid.Row="2" HorizontalAlignment="Center" Text="Number of mine" VerticalAlignment="Center" FontSize="18" ></TextBlock>
            <TextBox x:Name="MapHeightBox" IsEnabled="{x:Bind isCustom,Mode=OneWay}" Text="{x:Bind MapHeigh ,Mode=TwoWay}" Grid.Column="1" Margin="5.2,5,4,5" FontFamily="Segoe UI" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"></TextBox>
            <TextBox x:Name="MapWidthBox" IsEnabled="{x:Bind isCustom,Mode=OneWay}" Text="{x:Bind MapWidth ,Mode=TwoWay}" Grid.Row="1" Grid.Column="1" Margin="5.2,5,4,5" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"></TextBox>
            <TextBox x:Name="MapNumBox" IsEnabled="{x:Bind isCustom,Mode=OneWay}" Text="{x:Bind NumMine ,Mode=TwoWay}" Grid.Row="2" Grid.Column="1" Margin="5.2,5,4,5" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"></TextBox>
            <StackPanel Orientation="Horizontal" Grid.Row="3" VerticalAlignment="Top"  HorizontalAlignment="Center" Grid.ColumnSpan="2">
                <RadioButton Content="Customize" Checked="Custom_Click"></RadioButton>
            </StackPanel>

        </Grid>
    </Grid>
   

</Page>
